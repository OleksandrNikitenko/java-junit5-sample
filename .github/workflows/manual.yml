name: Manual workflow

on:
  workflow_dispatch
  
jobs:
  test:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    steps:
    - name: Run tests
      run: mvn clean test
